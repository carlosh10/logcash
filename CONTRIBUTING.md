# Contributing

- Use feature branches and open PRs into `main`.
- Keep generated outputs in `artifacts/` (ignored by git).
- Add/modify agents and tasks in `crew.yaml` only via PR.
- Link each PR to a task in Projects (Execution Plan).
